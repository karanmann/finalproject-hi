import { LoaderStyle, LoaderWrapper } from '../styles/LoaderStyles';

const Loader = () => {
    return (
        <LoaderWrapper>
            <LoaderStyle />
        </LoaderWrapper>
    );
};

export default Loader;
